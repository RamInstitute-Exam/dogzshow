-- INDEXES AND CONSTRAINTS SCRIPT
-- NOTE: Prisma ORM automatically embeds all Primary Keys, Foreign Keys, 
-- Unique Constraints, and Indexes directly into the schema.sql definition.

-- Example of the constraints applied via Prisma to the MySQL schema:
-- ALTER TABLE `EventRegistration` ADD CONSTRAINT `EventRegistration_eventId_fkey` FOREIGN KEY (`eventId`) REFERENCES `Event`(`id`) ON DELETE RESTRICT ON UPDATE CASCADE;
-- CREATE UNIQUE INDEX `KCICertificate_dogId_key` ON `KCICertificate`(`dogId`);
